# 配置文件

config = {
    # 每轮游戏题目总数
    "question_count": 40,
    # 截取区域左上角相对桌面的x坐标
    "projection_x": 10,
    # 截取区域左上角相对桌面的y坐标
    "projection_y": 72,
    # 截取区域的宽度
    "projection_width": 291,
    # 截取区域的高度
    "projection_height": 512,
    # 有效区域的顶部处于整张图片的比例
    "exp_area_top_rate": 0.37,
    # 有效区域的底部处于整张图片的比例
    "exp_area_bottom_rate": 0.61,
    # 正确区域的x坐标
    "tap_true_x": 85,
    # 错误区域的x坐标
    "tap_false_x": 225,
    # 正确和错误区域的y坐标
    "tap_y": 520,
    # 一个字符的宽度
    "single_char_width": 26
}